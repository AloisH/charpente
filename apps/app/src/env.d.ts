interface ImportMetaEnv {
  /** Dev-only quick login — mirror of SEED_ADMIN_EMAIL (see .env.example). */
  readonly VITE_SEED_ADMIN_EMAIL?: string;
  /** Dev-only quick login — mirror of SEED_ADMIN_PASSWORD. */
  readonly VITE_SEED_ADMIN_PASSWORD?: string;
}
